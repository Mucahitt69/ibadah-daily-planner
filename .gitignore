# Outils et réglages de l'assistant — ne doivent jamais être publiés
.claude/
.impeccable/
.agents/

# Notes de travail : elles restent sur l'ordinateur de Mucahid.
# HANDOFF.md liste ce qui n'est pas testé et ce qui bloque : c'est vrai,
# mais ça donnerait une image incomplète à quelqu'un qui tomberait dessus.
HANDOFF.md

# Le plan du passage en application Android (13 etapes), et le petit outil qui
# interroge l'appli en marche. Meme raison : ce sont des notes de travail.
PLAN-ANDROID.md
inspecter-appli.js

# Le brouillon de la fiche du Play Store : textes non arretes, a relire.
FICHE-PLAY-STORE.md

# Ancien dossier de publication et archives
site/
*.zip

# L'application Android (Capacitor).
# Le dossier android/ lui-meme EST versionne : il contiendra les icones et les
# reglages faits a la main. Seuls les dossiers refabriques a chaque
# construction sont exclus.
node_modules/
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/release/

# Le trousseau de signature : le perdre = ne plus jamais pouvoir mettre a jour
# l'application. Il ne doit JAMAIS partir sur GitHub.
*.jks
*.keystore
keystore.properties

# Fichiers temporaires
__pycache__/
*.pyc
Thumbs.db
desktop.ini
.DS_Store

# Les documents de relecture : du travail en cours, pas destine au depot.
# ⚠️ Un motif, et non trois noms : le 21 aout 2026 un quatrieme document est
# apparu (relecture-hadiths-entiers.docx) et serait parti sur GitHub tout seul.
# Ces fichiers portent des textes religieux NON RELUS -- voir le blocage n° 3.
relecture-*
